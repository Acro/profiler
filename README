Profiler project for PV173 ~ Grid computing group

---

How to compile demo.c example:

	gcc -finstrument-functions -g -c -o demo.o demo.c
	gcc -c -o trace.o trace.c
	gcc demo.o trace.o -o main

Run program with:

	./main

Output is located at:

	trace.out
